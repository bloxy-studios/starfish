[workspace]
resolver = "2"
members = ["crates/starfish-core", "src-tauri"]

[workspace.package]
version = "0.1.0"
license = "MIT OR Apache-2.0"
repository = "https://github.com/bloxy-studios/starfish"
edition = "2021"

[profile.release]
codegen-units = 1
lto = true
strip = true
